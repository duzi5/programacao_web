Function desmarcar ()
var listamarcados = document.getElementsByTagByName("input")
for (loop=0; loop < listamarcados.length; loop++) { 
    var item- listamarcados[loop]; 
    if (item.type == "checkbox" && item.checked){
        item.click()

    }
    }
}